import type { Config } from "tailwindcss";

const config: Config = {
  content: ["./src/**/*.{js,jsx,mdx,ts,tsx}"],
  darkMode: "class",
  plugins: [],
  theme: {
    extend: {},
  },
};

export default config;
